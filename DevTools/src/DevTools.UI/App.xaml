<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="DevTools.UI.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DevTools.UI">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Color Palette -->
            <Color x:Key="BackgroundColor">#1E1F26</Color>
            <Color x:Key="SurfaceColor">#282A36</Color>
            <Color x:Key="PrimaryColor">#826AF9</Color>
            <Color x:Key="AccentColor">#7B61FF</Color>
            <Color x:Key="SecondaryColor">#2CCDA9</Color>
            <Color x:Key="WarningColor">#FFD166</Color>
            <Color x:Key="ErrorColor">#EF476F</Color>
            <Color x:Key="TextPrimaryColor">#FFFFFF</Color>
            <Color x:Key="TextSecondaryColor">#A4A6B3</Color>
            <Color x:Key="DividerColor">#383B4F</Color>

            <!-- Brushes -->
            <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
            <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
            <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
            <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
            <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
            <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
            <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
            <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
            <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
            <SolidColorBrush x:Key="DividerBrush" Color="{StaticResource DividerColor}"/>

            <!-- Control Styles -->
            <Style x:Key="DefaultButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="20,10"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>

            <Style x:Key="SecondaryButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="Padding" Value="20,10"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>

            <Style x:Key="CardStyle" TargetType="Grid">
                <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                <Setter Property="CornerRadius" Value="12"/>
                <Setter Property="Padding" Value="16"/>
            </Style>

            <Style x:Key="HeadingStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                <Setter Property="FontSize" Value="24"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Margin" Value="0,0,0,16"/>
            </Style>

            <Style x:Key="SubheadingStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>

            <Style x:Key="BodyTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
            </Style>

            <Style x:Key="AccentTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
            </Style>

            <Style x:Key="NavigationViewStyle" TargetType="NavigationView">
                <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
                <Setter Property="IsBackButtonVisible" Value="Collapsed"/>
                <Setter Property="IsPaneToggleButtonVisible" Value="False"/>
                <Setter Property="PaneDisplayMode" Value="Left"/>
                <Setter Property="IsSettingsVisible" Value="False"/>
            </Style>

            <!-- Custom Control Templates -->
            <ControlTemplate x:Key="ToolCardTemplate" TargetType="GridViewItem">
                <Grid Background="{StaticResource SurfaceBrush}" 
                      CornerRadius="12" 
                      Width="260" 
                      Height="180"
                      Padding="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding Name}" 
                                   Style="{StaticResource SubheadingStyle}" 
                                   Grid.Column="0"/>

                        <Border Grid.Column="1" 
                                Background="{StaticResource BackgroundBrush}" 
                                CornerRadius="6" 
                                Padding="8,4" 
                                Visibility="{Binding IsPremium, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="Premium" 
                                       Foreground="{StaticResource WarningBrush}" 
                                       FontSize="12" 
                                       FontWeight="SemiBold"/>
                        </Border>
                    </Grid>

                    <TextBlock Text="{Binding Description}" 
                               Style="{StaticResource BodyTextStyle}" 
                               Grid.Row="1" 
                               Margin="0,8,0,0"/>

                    <StackPanel Orientation="Horizontal" 
                                Grid.Row="2" 
                                HorizontalAlignment="Right">
                        <Button Content="Open" 
                                Style="{StaticResource DefaultButtonStyle}" 
                                Margin="0,0,8,0" 
                                FontSize="12"/>
                        <Button Content="♥" 
                                Style="{StaticResource SecondaryButtonStyle}" 
                                Width="36" 
                                Padding="0" 
                                FontSize="16"/>
                    </StackPanel>
                </Grid>
            </ControlTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
