<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DevTools.UI.Views.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DevTools.UI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <NavigationView x:Name="NavigationView" 
                        SelectionChanged="NavigationView_SelectionChanged"
                        IsSettingsVisible="False">
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Content="Dashboard" Tag="dashboard" />
                <NavigationViewItem Icon="Favorite" Content="Favorites" Tag="favorites" />
                <NavigationViewItem x:Name="AdminItem" Icon="Admin" Content="Admin" Tag="admin" Visibility="Collapsed" />
            </NavigationView.MenuItems>

            <NavigationView.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="HeaderText" Style="{StaticResource TitleTextBlockStyle}" Margin="12,0"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button x:Name="LoginButton" 
                                Content="Login" 
                                Click="LoginButton_Click"
                                Margin="12,0"/>
                        <Button x:Name="LogoutButton" 
                                Content="Logout" 
                                Click="LogoutButton_Click" 
                                Visibility="Collapsed"
                                Margin="12,0"/>
                        <Button x:Name="UpgradeButton" 
                                Content="Upgrade to Premium" 
                                Click="UpgradeButton_Click" 
                                Visibility="Collapsed"
                                Margin="12,0"/>
                    </StackPanel>
                </Grid>
            </NavigationView.Header>

            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>
</Page>
